# Generated cache of flagged Outlook emails — contains email subjects/senders.
# Machine-specific and potentially sensitive; never commit.
flagged_emails_cache.json

# Queued unflag requests (Outlook entry IDs) awaiting the nightly write-back.
pending_unflags.json

# Daily Claude-generated assistant briefing — built from your tasks/emails.
assistant_briefing_cache.json

# Python bytecode
__pycache__/
*.pyc

# Temp files
_temp_shortcut.ps1
*_err.log
